# DEVLOG - Dark Current MLX Pilot

- **2026-06-18**: Inizializzata la root directory locale in base agli accordi via email/LinkedIn con H. Usami. Creato lo schema di output `schema.json` focalizzato su riproducibilità (seed, temp 0, HW config) e selezionati i 3 modelli per il pilot (Llama-3-8B, Mistral-7B-v0.3, Qwen2-7B). Attesa ok finale prima di implementare il codice Python/MLX.
- **2026-06-26**: Refactor da script monolitico a package installabile. Aggiunti CLI, runner mock/MLX, JSONL input/output, summary JSON, metadati hardware, dataset sample e test offline. Scope ancora pilot: dataset reale e schema definitivo da confermare con Usami prima di pubblicare risultati come collaborazione.
- **2026-06-27**: Design note budget-aware (ingest Noam Brown / No Priors): l'eval non deve riportare solo preference/accuracy ma anche budget inferenziale, scaffold e routing. Aggiunto alla roadmap (`inference_budget_tokens`, `wall_clock_s`, scaffold/routing) — nessuna modifica al codice.
- **2026-06-29**: Portfolio-grade pass. Verificato il refactor end-to-end: 8/8 test offline verdi (~0.02s, no network/MLX); due mock baseline eseguiti come sanity check della metrica — `position` (giudice 100% position-biased) → `dark_current_score 1.0`, `tie` (order-invariant) → `0.0`. README riscritto a livello portfolio (blocco Results con numeri reali, repo structure, status/roadmap, scope onesto senza over-claim di collaborazione/NeurIPS). Commit locale del package + test + dataset + schema (prima era solo l'Initial commit monolitico). MLX reale non lanciato (nessun download non confermato); push pubblico lasciato alla conferma di Orazio.
